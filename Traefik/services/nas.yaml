tcp :
  services :
    nas :
      loadBalancer :
        servers :
           - address : "192.168.1.250:443"

  routers :
    nas :
      entryPoints : 
        - "websecure"
      rule : "HostSNI(`telechargement.revol.site`) || HostSNI(`audio.revol.site`) || HostSNI(`nas.revol.site`) || HostSNI(`video.revol.site`) || HostSNI(`drive.revol.site`) || HostSNI(`photo.revol.site`) || HostSNI(`file.revol.site`)"
      service : nas
      tls:
        passthrough: true

